<h1> <%= @album.title %></h1>


<h3>Tracks</h3>
<ul>
  <% @album.tracks.each do |track| %>
    <li><%= link_to "#{track.title}", track_url(track) %></li>
  <% end %>
</ul>

<%= link_to "Add a Track", new_album_track_url(@album) %>


<%= link_to "Back to Band", band_url(@album.band_id) %>
